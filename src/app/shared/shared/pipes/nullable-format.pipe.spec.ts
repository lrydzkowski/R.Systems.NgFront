import { NullableFormatPipe } from './nullable-format.pipe';

describe('NullableFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new NullableFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
